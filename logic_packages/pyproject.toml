[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "dabs_local_package"
version = "0.1.0"
description = "Unified transform data logic"

[tools.setuptools.packages.find]
where = ["src"]
